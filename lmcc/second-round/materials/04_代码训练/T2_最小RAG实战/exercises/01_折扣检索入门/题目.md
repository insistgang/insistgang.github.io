# 练习 01：折扣检索入门

难度：基础。先确认你能把“查询 → 相似题 → 当前题”串起来。

请用共享的 `starter/submission.py` 完成 retrieval、用户消息和 JSON Prompt。数据在 `data/`，最终答案在 `../../answers/`。

```bash
python tools/batch_evaluate.py \
  --exercise exercises/01_折扣检索入门 \
  --answers answers/01_折扣检索入门_expected.jsonl
```

本地 mock embedding 只验证代码管线。正式考试由评测器提供真实 embedding 函数。
